server {
    listen 80 default_server;
    server_name _;
    server_tokens off;

    autoindex off;
    root /srv;

    location = /manifest.json {
        # served read-only to clients; crond (same container) writes it
        try_files /manifest.json =404;
    }
    location / {
        return 404;
    }
}
